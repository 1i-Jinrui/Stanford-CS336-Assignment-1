##### 关于线性层wight为什么要定义形状为(feature_out, feature_in)，后续矩阵乘法时再转置的问题
* 在计算机内存中，无论是 CPU 还是 GPU，多维张量都是被“拉平”存储在一维的线性内存条中的。PyTorch 默认使用行优先（Row-major）的内存布局。 当定义 (out_features, in_features) 时，每一行的 in_features 个元素在物理内存中是连续的。 
* 在计算矩阵乘法时，输出的每一个神经元（对应 out_features 中的一个维度），本质上是用输入的特征向量 x 去和权重矩阵的某一行做点积。 因为这一行的数据在内存中是连续的，CPU/GPU 的缓存机制可以一次性把这整行数据高效地抓取到寄存器中完成计算。
反之：如果将权重定义为 (in_features, out_features)，在计算某个输出神经元时，需要去读取矩阵的一列。在行优先的内存中，按列读取意味着内存指针需要不断地“跳跃”，大幅拖慢计算速度。
##### 关于RMSNorm层，为什么需要将输入提升为 float32 类型再进行平方运算
* 保证数值稳定性，具体可以拆解为防止溢出和减少累加精度损失。如果在 FP16 下直接计算 x<sup>2</sup>，只要 x 的绝对值大于 256，计算结果就会直接溢出变成 inf。此外，累加数千个元素的平方值会使总和迅速变大，如果直接使用尾数精度极低的 bfloat16 或 float16 进行计算，会引发“大数吃小数”的截断现象——即累加池变大后，后续较小的值在相加时会因超出尾数表示范围而被直接舍弃，导致最终算出的 RMS 值严重偏小。
##### 为什么RoPE不会污染语义
* RoPE 不太污染语义，是因为它不是把位置当作额外向量直接加到语义上，而是对语义向量做保持范数的旋转变换，把位置信息编码为“相位”而非“内容叠加”；同时它主要作用于注意力中的 Q 和 K，服务于相对位置关系建模，因此比直接加法位置编码更少破坏原有语义结构。

为什么用 UTF-8 bytes 训练 tokenizer，而不是 UTF-16/UTF-32？

UTF-8 是一种变长编码，对英文、数字和常用符号等只用 1 个字节表示，而像汉字等字符通常用 3 个字节。相比之下，UTF-16 至少需要 2 个字节，UTF-32 固定为 4 个字节。对于以英文为主的语料库，UTF-8 的存储和处理效率远高于后两者。

一个分词器的词汇表(Vocabulary)大小是有限的。如果直接在 Unicode 字符 (UTF-32) 上操作，遇到词汇表中没有的字符（例如一个新的 Emoji 或一个罕见的汉字），就只能将其标记为 &lt;UNK&gt;。而基于 UTF-8 字节的分词器，其基础词汇表是固定的 256 个字节（从 0x00 到 0xFF）。任何未知的、罕见的字符，甚至是乱码，都可以被分解成一串已知的字节序列来表示。这样就从根本上消除了 &lt;UNK&gt; 符号，使得模型能够处理任何形式的文本输入，而不会丢失信息。

---

不同分词方法对比：

| 类型                       | 切分单位        | 主要优点                     | 主要缺点                     |
| ------------------------ | ----------- | ------------------------ | ------------------------ |
| **词级（Word-level）**       | 完整单词        | 语义完整、直观、序列较短             | 词汇表巨大；OOV问题严重；难以处理词形变化   |
| **字符级（Character-level）** | 单个字符        | 词汇表较小（十几万）；基本没有OOV问题          | 序列很长；单个字符语义有限；学习效率较低 词表稀疏，很多生僻字符几乎用不到    |
| **字节级（Byte-level）**      | 0～255的原始字节  | 词汇表固定为256；可表示任何文本；完全无OOV | 序列更长；语义高度破碎；模型学习难度大      |
| **子词级（Subword-level）**   | 完整词与字符之间的子词 | 平衡词汇表大小与序列长度；能处理新词和词形变化，词表大概在3-10万  | 分词器需要额外训练；词汇表大小和合并规则需要调参 |


transformer_accounting：前向 FLOPs 包括哪些矩阵乘法？

transformer_accounting：哪些部分 FLOPs 最大？

transformer_accounting：模型变大时 FLOPs 占比如何变化？

adamwAccounting：AdamW 训练内存如何分解？
